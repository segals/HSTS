<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

  <modelVersion>4.0.0</modelVersion>

  <groupId>hsts</groupId>
  <artifactId>hsts-parent</artifactId>
  <version>1.0.0</version>
  <packaging>pom</packaging>

  <name>HSTS - High School Test System</name>
  <description>Software Engineering 203.3140, Group 1. Client-server exam management system.</description>

  <!--
      The four modules map onto the three tiers of the submitted class diagram:

        hsts-ocsf    the reused OCSF communication framework (external reuse)
        hsts-common  Data tier  <<Entity>> + the message protocol - shipped in BOTH jars
        hsts-server  Application tier <<Control>> + boundaries to external systems
        hsts-client  Presentation tier <<Boundary>>
  -->
  <modules>
    <module>hsts-ocsf</module>
    <module>hsts-common</module>
    <module>hsts-server</module>
    <module>hsts-client</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>26</maven.compiler.release>

    <!-- Versions pinned once here; no module repeats a version number. -->
    <javafx.version>26.0.2</javafx.version>
    <mysql.version>9.7.0</mysql.version>
    <pdfbox.version>3.0.8</pdfbox.version>
    <poi.version>5.5.1</poi.version>

    <compiler.plugin.version>3.15.0</compiler.plugin.version>
    <shade.plugin.version>3.6.2</shade.plugin.version>

    <!--
        JavaFX ships its native libraries per operating system, so every
        JavaFX artifact must be requested with a platform classifier.
        Both project laptops are Windows, so this is "win".
        This does mean the produced jars are Windows-only - a stated
        limitation of the project, not an accident.
    -->
    <javafx.platform>win</javafx.platform>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>hsts</groupId>
        <artifactId>hsts-ocsf</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>hsts</groupId>
        <artifactId>hsts-common</artifactId>
        <version>${project.version}</version>
      </dependency>

      <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-base</artifactId>
        <version>${javafx.version}</version>
        <classifier>${javafx.platform}</classifier>
      </dependency>
      <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-graphics</artifactId>
        <version>${javafx.version}</version>
        <classifier>${javafx.platform}</classifier>
      </dependency>
      <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-controls</artifactId>
        <version>${javafx.version}</version>
        <classifier>${javafx.platform}</classifier>
      </dependency>
      <dependency>
        <groupId>org.openjfx</groupId>
        <artifactId>javafx-fxml</artifactId>
        <version>${javafx.version}</version>
        <classifier>${javafx.platform}</classifier>
      </dependency>

      <dependency>
        <groupId>com.mysql</groupId>
        <artifactId>mysql-connector-j</artifactId>
        <version>${mysql.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-shade-plugin</artifactId>
          <version>${shade.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>

</project>
